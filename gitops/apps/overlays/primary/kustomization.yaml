apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# A base já aponta para gitops/workloads/overlays/primary/<serviço> por
# padrão — este overlay existe só para manter a mesma estrutura simétrica
# do overlay dr (e ser o alvo padrão do root-app.yaml).
resources:
  - ../../base
