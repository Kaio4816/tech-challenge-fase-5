apiVersion: v1
kind: ServiceAccount
metadata:
  name: volunteer-service
  annotations:
    # Role criada pelo módulo irsa (Fase 3). Valor do overlay primary
    # (name_prefix=solidarytech-primary); o overlay dr troca para
    # solidarytech-dr-volunteer-service.
    eks.amazonaws.com/role-arn: arn:aws:iam::712819938659:role/solidarytech-primary-volunteer-service
