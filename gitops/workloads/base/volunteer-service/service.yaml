apiVersion: v1
kind: Service
metadata:
  name: volunteer-service
  labels:
    app.kubernetes.io/name: volunteer-service
spec:
  selector:
    app.kubernetes.io/name: volunteer-service
  ports:
    - name: http
      port: 8083
      targetPort: http
