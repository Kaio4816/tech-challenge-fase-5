apiVersion: v1
kind: ServiceAccount
metadata:
  name: ngo-service
  # Sem annotation IRSA: o ngo-service só fala com o Postgres, não chama
  # nenhuma API da AWS (diferente de donation-service/volunteer-service).
