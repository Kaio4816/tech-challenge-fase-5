apiVersion: v1
kind: ServiceAccount
metadata:
  name: donation-service
  annotations:
    # Role criada pelo módulo irsa (Fase 3, infra/terraform/modules/irsa) —
    # nome determinístico (name_prefix + "-donation-service"), sem sufixo
    # aleatório, por isso pode ficar hardcoded aqui. Valor do overlay
    # primary (name_prefix=solidarytech-primary); o overlay dr troca para
    # solidarytech-dr-donation-service.
    eks.amazonaws.com/role-arn: arn:aws:iam::712819938659:role/solidarytech-primary-donation-service
